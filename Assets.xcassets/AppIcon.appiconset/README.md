# App Icon placeholders

Binary image uploads are blocked in this environment. Add your real app-icon PNGs directly into this `.appiconset` from Xcode to resolve the missing asset warnings. The `Contents.json` already declares all required sizes.
